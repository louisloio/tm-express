DATABASE_URL="postgresql://YOUR_MAC_USERNAME@localhost:5432/tm_express_dev?schema=public"
PORT=4000
# Generate with: openssl rand -hex 32
ENCRYPTION_KEY=
# From https://console.anthropic.com/settings/keys — used to classify email
# attachments for auto-filing. Enter it directly here, never in chat.
ANTHROPIC_API_KEY=
